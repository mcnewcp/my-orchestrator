# Repository guidance

Keep changes focused on the approved issue and follow the conventions in nearby code.
Add regression tests for changed behavior. Preserve existing tests unless the approved
scope explicitly justifies a change to their coverage.

Factory role instructions define the current stage. Python owns verification commands,
Git commits, and GitHub writes; an implementation session edits source and tests.
